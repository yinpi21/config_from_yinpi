[config]
reload_apps = true
# Touch alacritty.toml pour déclencher le rechargement des couleurs
run_after = [["touch", "~/.config/alacritty/alacritty.toml"]]

[config.reload_apps_list]
gtk_theme = true

# ── i3 ────────────────────────────────────────────────────────────────────────
[templates.i3]
input_path  = '~/.config/matugen/templates/i3.conf'
output_path = '~/.config/i3/colors.conf'
post_hook   = 'i3-msg reload > /dev/null'

# ── Polybar ───────────────────────────────────────────────────────────────────
[templates.polybar]
input_path  = '~/.config/matugen/templates/polybar.ini'
output_path = '~/.config/polybar/colors.ini'
post_hook   = 'polybar-msg cmd restart > /dev/null'

# ── Rofi ──────────────────────────────────────────────────────────────────────
[templates.rofi]
input_path  = '~/.config/matugen/templates/rofi.rasi'
output_path = '~/.config/rofi/colors.rasi'

# ── Alacritty ─────────────────────────────────────────────────────────────────
[templates.alacritty]
input_path  = '~/.config/matugen/templates/alacritty.toml'
output_path = '~/.config/alacritty/colors.toml'

# ── Dunst ─────────────────────────────────────────────────────────────────────
[templates.dunst]
input_path  = '~/.config/matugen/templates/dunstrc'
output_path = '~/.config/dunst/dunstrc'
post_hook   = 'dunstctl reload'

# ── Flameshot ─────────────────────────────────────────────────────────────────
[templates.flameshot]
input_path  = '~/.config/matugen/templates/flameshot.ini'
output_path = '~/.config/flameshot/flameshot.ini'

# ── GTK 3 ─────────────────────────────────────────────────────────────────────
[templates.gtk3]
input_path  = '~/.config/matugen/templates/gtk.css'
output_path = '~/.config/gtk-3.0/colors.css'

# ── GTK 4 ─────────────────────────────────────────────────────────────────────
[templates.gtk4]
input_path  = '~/.config/matugen/templates/gtk.css'
output_path = '~/.config/gtk-4.0/colors.css'

# ── Qt 5 ──────────────────────────────────────────────────────────────────────
[templates.qt5ct]
input_path  = '~/.config/matugen/templates/qtct.conf'
output_path = '~/.config/qt5ct/colors/matugen.conf'

# ── Qt 6 ──────────────────────────────────────────────────────────────────────
[templates.qt6ct]
input_path  = '~/.config/matugen/templates/qtct.conf'
output_path = '~/.config/qt6ct/colors/matugen.conf'
